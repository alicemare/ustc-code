/*
 * List & tranself(List & A):
 * 	int max_index = length(A)-1
 * 	for ( int i = 0 ; i < max_index ; ++i):
 * 		swap(A[i],A[max_index-i]
 * 	return A
 *
 * void swap( int & a ; int & b ):
 * 	int temp = b
 * 	b = a
 * 	a = temp
 */
